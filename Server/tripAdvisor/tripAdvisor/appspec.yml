version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/action
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
#경로
hooks:
  ApplicationStart:
    - location: scripts/trip.sh
      timeout: 60
      runas: ubuntu